// Funzione per ricevere le transazioni ricorrenti
const getRecurrentTransactions = async () => {};

// Funzione per aggiungere la transazione ricorrente
const postRecurrentTransactions = async () => {};

// Funzione per modificare la transazione ricorrente
const patchRecurrentTransaction = async () => {};

// Funzione per eliminare la transazione ricorrente
const deleteRecurrentTransaction = async () => {};

// Esportazione funzioni
export {
    getRecurrentTransactions,
    postRecurrentTransactions,
    patchRecurrentTransaction,
    deleteRecurrentTransaction,
};
